import React from 'react'

const Summary = ({handleCloseCheckOut , handleIncrea }) => {
  return (
    <>
    <button onClick={handleCloseCheckOut}>dong</button>
     Summary
    <button onClick={handleIncrea}>continue</button>
  </>
  )
}

export default Summary